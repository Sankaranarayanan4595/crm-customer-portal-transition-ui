import { RemoveHyphenSpacesPipe } from './remove-hyphen-spaces.pipe';

describe('RemoveHyphenSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveHyphenSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
