import { AgGridDynamicHeightDirective } from './ag-grid-dynamic-height.directive';

describe('AgGridDynamicHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new AgGridDynamicHeightDirective();
    expect(directive).toBeTruthy();
  });
});
