import { BbTableDynamicHeightDirective } from './bb-table-dynamic-height.directive';

describe('BbTableDynamicHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new BbTableDynamicHeightDirective();
    expect(directive).toBeTruthy();
  });
});
