import { ContainerHeightDirective } from './container-height.directive';

describe('ContainerHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerHeightDirective();
    expect(directive).toBeTruthy();
  });
});
